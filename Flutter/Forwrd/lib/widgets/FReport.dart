import 'package:flutter/material.dart';

class FReport extends StatelessWidget {
  const FReport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Report the motherf***ing user'),
    );
  }
}
